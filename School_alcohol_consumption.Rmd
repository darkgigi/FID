---
title: "School_alcohol_consumption"
output: html_document
date: "2024-11-10"
---
# Consumo de alcohol en estudiantes

# Pasos KDD
# Integración

```{r}
install.packages("dplyr")
library(dplyr)
```

```{r}

# Lectura de dataset
df1=read.table("data/student-mat.csv",sep=";",header=TRUE)
df1 <- df1 %>% 
  mutate(subject = "math")
df2=read.table("data/student-por.csv",sep=",",header=TRUE)
df2 <- df2 %>% 
  mutate(subject = "portuguese")


df=rbind(df1,df2)
print(nrow(df)) # 382 students
print(head(df))
print(str(df))
```



